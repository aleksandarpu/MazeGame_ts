{
  "compilerOptions": {
    "target": "ES2022",                          /* Specify ECMAScript target version */
    "lib": ["ES2022", "DOM", "ESNext.Temporal"], /* Include Temporal type declarations used by Firebase */
    "module": "ESNext",                        /* Specify module code generation */
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,                   /* Allow importing assets/data/*.json */
    "noEmit": true,                              /* Type-check only; Vite does the bundling */
    "isolatedModules": true,                     /* Vite/esbuild compiles each file on its own */
    "types": ["node", "vite/client"],            /* Node typings + Vite asset imports (*.png) */
    "strict": true,                              /* Enable all strict type-checking options */
    "esModuleInterop": true                      /* Emit additional JavaScript to ease support for importing CommonJS modules */
  },
  "include": ["src"]
}
